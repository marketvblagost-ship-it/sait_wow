const menuBtn=document.getElementById('menuBtn');const mainNav=document.getElementById('mainNav');if(menuBtn){menuBtn.addEventListener('click',()=>mainNav.classList.toggle('open'))}document.querySelectorAll('.filter').forEach(btn=>{btn.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;document.querySelectorAll('.portfolio-card').forEach(card=>{card.style.display=(f==='all'||card.dataset.cat.includes(f))?'block':'none'})})});function showToast(text){const t=document.getElementById('toast');if(!t)return;t.textContent=text;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),3000)}document.querySelectorAll('.order-similar').forEach(btn=>{btn.addEventListener('click',()=>{const niche=btn.dataset.niche||'';const input=document.getElementById('businessNiche');if(input)input.value=niche;document.getElementById('contact')?.scrollIntoView({behavior:'smooth'});showToast('Ниша подставлена в форму. Оставьте контакты для расчёта.')})});